<div class="w-full">
 <%= render partial: 'shared/notifications', locals: { message: flash[:notice], message_type: 'notice'} if notice.present? %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Service offers</h1>
    <%= link_to "New service offer", new_service_offer_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="service_offers" class="min-w-full">
    <%= render @service_offers %>
  </div>
</div>
