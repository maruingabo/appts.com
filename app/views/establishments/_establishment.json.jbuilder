json.extract! establishment, :id, :name, :address, :phone_number, :subdomain, :domain, :status, :booking_offset, :city_id, :created_at, :updated_at
json.url establishment_url(establishment, format: :json)
